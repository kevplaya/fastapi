[tool.poetry]
name = " wanted"
version = "0.1.0"
description = "company api based on multilingual"
authors = ["jihoon.lee"]
readme = "README.md"

[tool.poetry.dependencies]
python = "3.13.3"
fastapi = "^0.115.12"
sqlalchemy = "^2.0.40"
pymysql = "^1.1.1"
python-dotenv = "^1.1.0"
pydantic = "^2.11.3"
uvicorn = "^0.34.2"
gunicorn = "^23.0.0"

[tool.poetry.group.dev.dependencies]
black = "^25.1.0"
flake8 = "^7.2.0"
isort = "^6.0.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
